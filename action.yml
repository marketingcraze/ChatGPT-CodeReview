name: ChatGPT CodeReviewer
description: 'An evidence-backed, context-validated code review Action'
branding:
  icon: 'gift'
  color: orange
inputs:
  review_mode:
    description: 'Review stage: initial or final'
    required: false
    default: 'initial'
  policy_path:
    description: 'Repository-relative path to a private caller-owned review policy'
    required: false
  previous_review_run_path:
    description: 'Repository-relative path to a trusted exact-SHA initial ReviewRun JSON artifact'
    required: false
  publish_review_comment:
    description: 'Publish the managed Action review body to the pull request timeline'
    required: false
    default: 'true'
  trusted_context_manifest_path:
    description: 'Repository-relative trusted context manifest whose document hashes are verified before use'
    required: false
  ci_evidence_path:
    description: 'Repository-relative exact-SHA GitHub Checks and Statuses evidence JSON'
    required: false
  gitnexus_binary_path:
    description: 'Verified GitNexus 1.6.9 executable inside GITHUB_WORKSPACE; defaults to pinned npx compatibility mode'
    required: false
  gitnexus_version:
    description: 'Permitted GitNexus version; only 1.6.9 is accepted by this release'
    required: false
    default: '1.6.9'
  initial_model:
    description: 'Initial evidence discovery model'
    required: false
    default: 'gpt-5.6-luna'
  validation_model:
    description: 'Low-cost routine validation model'
    required: false
    default: 'gpt-5.6-luna'
  context_validation_model:
    description: 'Context or disagreement validation model'
    required: false
    default: 'gpt-5.6-terra'
  escalation_model:
    description: 'High/critical unresolved escalation model'
    required: false
    default: 'gpt-5.6-sol'
  max_patch_length:
    description: 'Maximum reviewable patch length; exceeding it produces insufficient_evidence'
    required: false
    default: '30000'
  max_context_requests:
    description: 'Maximum targeted GitNexus requests per run'
    required: false
    default: '6'
  fail_on_verdict:
    description: 'Fail the workflow unless the verdict is approved_to_merge'
    required: false
    default: 'false'
outputs:
  verdict:
    description: 'approved_to_merge, changes_required, or insufficient_evidence'
  reviewed_sha:
    description: 'Exact pull request head SHA reviewed'
  gitnexus_status:
    description: 'Freshness state of the exact-SHA GitNexus index'
  findings_json:
    description: 'Structured validated findings without source excerpts'
  review_run_json:
    description: 'Structured ReviewRun without policy, context payloads, or source excerpts'
  timings_json:
    description: 'Non-sensitive stage timing records'
  model_usage_json:
    description: 'Aggregate model token usage without prompts or responses'
  gitnexus_restore_source:
    description: 'exact_artifact, base_cache, or cold'
runs:
  using: 'node24'
  main: 'action/index.cjs'
